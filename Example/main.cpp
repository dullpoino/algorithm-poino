#include<algorithm-poino.hpp>

////////////////////////////////////////////////////////////////

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    ////////////////////////////

    // 素数列挙
    //Math::PRIME p(100, 2);
    //outVL(p.getPrime());

    // MOD
    //Math::modint x = 998244354;
    //outL(x);
    //x -= 2;
    //outL(x);
    //x *= x;
    //outL(x);

    return 0;
}
